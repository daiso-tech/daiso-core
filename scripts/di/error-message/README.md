# DI Error Message Scripts

These scripts exist to **review the quality of the error messages** that the dependency injection container produces.

Each script intentionally uses the container in a **wrong way** to trigger a specific error. The thrown error is then inspected manually to judge whether its message is clear and understandable for an end user.
