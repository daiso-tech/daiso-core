---
"@daiso-tech/core": minor
---

Now the instance `validate` method of `ListCollection`, `IterableCollection`, and `AsyncIterableCollection` classes throws `ValidationError`.
It is also documented in the `ICollection` and `IAsyncCollection` contract that the method throws.
