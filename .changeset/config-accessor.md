---
"@daiso-tech/core": minor
---

Introduced new component `@daiso-tech/core/config-accessor`, including the `ConfigAccessor` class and the `IConfigAccessor` contract. This component provides a type-safe way to read domain configuration values by path, with support for optional schema validation and default values.
